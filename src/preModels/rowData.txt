unction MobileChange(e){ setUserDetails({

UserName: userDetails.UserName,

Mobile: e.target.value

})

if(userDetails.Mobile!=""){

setErrors({ UserError: errors.UserError, MobileError: "

})

function SubmitClick(e){ e.preventDefault(); if(userDetails.UserName== "") { UserError: 'User Name Required', MobileError: errors.MobileError })

setErrors({

} else if(userDetails. Mobile==""){ UserError: errors.UserError, MobileError: 'Moble Required'

setErrors({

}

}

function Verify Mobile(e){

var pattern = /\+91\d{10}/; if(e.target.value.match(pattern)){ MobileError: "

setErrors({

}) } else {

setErrors({

MobileError:'Invalid Mobile'

})